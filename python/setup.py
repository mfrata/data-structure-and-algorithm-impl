import setuptools


setuptools.setup(
    name="dsalgo",
    version="0.1.0",
    packages=setuptools.find_packages(exclude=['tests*']),
    description="My implemetations of ds and algo in python",
    author="@mfrata",
)
